box:
  id: sakuna63/wercker-container-android
  username: $USERNAME
  password: $PASSWORD
  tag: latest
build:
  steps:
    - script:
      name: run gradle assembleDebug
      code: |
        ./gradlew --project-cache-dir=$WERCKER_CACHE_DIR assemble testDebugUnitTest -PdisablePreDex
